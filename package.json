{
  "name": "cs2-obs-pov-router",
  "version": "0.8.0",
  "private": true,
  "scripts": {
    "start": "npm.cmd --prefix server start",
    "server": "npm.cmd --prefix server start",
    "build": "npm.cmd --prefix client run build"
  }
}
